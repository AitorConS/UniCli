{
  "name": "nextapp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && node scripts/postbuild.js",
    "start": "node .next/standalone/server.js"
  },
  "dependencies": {
    "next": "15.5.21",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "//": "next pins postcss exactly and constrains sharp to ^0.34, so neither can pick up its security fixes on its own. Drop these overrides once a next release ships the patched versions.",
  "overrides": {
    "postcss": "^8.5.18",
    "sharp": "^0.35.3"
  }
}
